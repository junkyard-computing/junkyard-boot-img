# Ignore every file in this folder
*
# Except these
!.gitignore
!_enter
!packages.txt
!debian_snapshot
!kmscon.env
!overlay
!overlay/**
# ...but not the cross-compiled pixel-bootctl binary the .build_pixel_bootctl
# Make target drops in (re-ignore it; source-of-truth is the
# tools/pixel-bootctl submodule). Must come after !overlay/** to win.
overlay/usr/local/bin/pixel-bootctl