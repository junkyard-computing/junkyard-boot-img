# ARM NDA blob recipients — who can decrypt secrets/arm-mali-blobs.tar.age.
#
# One PUBLIC key per line. Both formats work (age accepts SSH pubkeys directly):
#   - SSH:  ssh-ed25519 AAAA... user@host      (preferred — use existing ~/.ssh keys)
#   - age:  age1...                            (from `age-keygen`)
#
# These are PUBLIC keys — safe to commit. Each builder keeps their matching
# PRIVATE key (e.g. ~/.ssh/id_ed25519) and never shares it.
#
# To grant someone access: add their pubkey line, then re-pack + commit:
#   just pack_arm_blobs <staging-dir>
#   git add secrets/recipients.txt secrets/arm-mali-blobs.tar.age
# To revoke: delete their line and re-pack (they can't open the new blob; the
# committed blobs are static NDA libs, so rotate the content too if it matters).

ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIF2Z7LbaDPTNkdnuvFivXTUx8X9gU0ZyWrrYBH7KSmG3 chris@chris-laptop
